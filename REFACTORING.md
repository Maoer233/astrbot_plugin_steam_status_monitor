# 项目重构说明

## 重构背景

原项目的插件编排、Steam API 调用、数据持久化、图片渲染和管理接口等主要逻辑集中在根目录 `main.py` 中，职责边界不够清晰，模块之间耦合度较高，增加了维护、测试和后续扩展的成本。

本次修改不新增或改变插件功能，主要目标是在保持现有入口、指令行为及本地数据兼容的前提下，重新梳理项目结构并提升管理接口性能和运行稳定性。

## 主要改动

### 1. 模块化重构

- 将根目录 `main.py` 调整为轻量兼容入口。
- 将插件主体迁移至 `src/plugin/steam_status_monitor.py`。
- 按职责拆分应用服务、领域逻辑、Steam API 与网络基础设施、数据持久化、图片渲染、管理接口和通用工具。
- 将字体、默认图片和文案资源统一整理至 `assets/`。
- 补充模块结构测试，防止业务逻辑再次集中到入口文件。

### 2. 管理接口性能优化

- 将 Dashboard、群组统计、玩家搜索和热力图等统计构建逻辑拆分至独立模块。
- 增加异步 TTL 响应缓存。
- 合并相同缓存键的并发请求，减少重复计算。
- 在相关写操作完成后使对应缓存失效，避免返回过期数据。
- 将较重的同步统计计算移出事件循环，降低其对其他异步任务的影响。

### 3. 稳定性与故障诊断

- 完善网络请求和异常日志，保留更完整的错误上下文。
- 改进多群状态处理，降低单个群或单个玩家异常对全局轮询任务的影响。
- 补充网络及日志工具相关单元测试。
- 修复模块化入口迁移后 AstrBot 指令处理器绑定丢失的问题。

### 4. 文档与本地数据保护

- 补充项目结构、功能位置、数据边界及主要维护入口说明。
- 将插件本地数据目录加入忽略规则，避免运行数据被意外提交。
- 更新开发、测试和贡献相关说明。

## 兼容性

- 保留根目录 `main.py` 作为 AstrBot 插件兼容入口。
- 不改变现有指令的主要使用方式。
- 不主动修改现有本地数据结构和数据目录。
- 不包含趣味日报等实验性或 Fork 专属功能。

## 验证情况

该版本已经在本地 AstrBot 实例中持续运行并进行实际功能验证。插件加载、指令绑定、Steam 状态轮询、多群处理、状态通知、排行榜及管理页面等现有功能均可正常使用，重启后原有群组、玩家绑定、通知会话和缓存数据仍可继续使用。
